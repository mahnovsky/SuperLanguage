fn print(a, b)
{
	let f = 5 + b;
	let p = "fn test " + a;
	p = p + " hi";
	{
		__print("a: ", a, " b: ", b);
		#p = b - 1;
		
		__print("f: ", f);
	}
	__dump_callstack();
	return b + 45;
}

fn foo(t, k)
{
	let h = "foo" + k;
	let y = t * 3;

	prints(h);

	return y;
}
let res = foo(11, "test");
#__print("foo res: ", res);
let s = "hello" + " world";
__print(s);
s = s + " 1134";
let x = 12;
let y = 3 * ((x + 2) * (9 - 10));
y = 2.5 + 1;

let test = x * y / 9;
{
	let g = print(s, x);
	__print("print res: ", g);
	let miha = 15.0 / x;
	x = miha * y;
	{
		miha = 0;
	}
}

#let miha = 1 > 4;
