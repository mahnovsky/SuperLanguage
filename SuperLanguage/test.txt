fn print(a, b)
{
	let p = "fn test " + a;
	p = p + " hi";

	#p = b - 1;

	prints(b, " ", p);

	return b + 5;
}

fn foo(t)
{
	let h = "foo";
	let y = t * 3;

	prints(h);

	return y;
}
let res = foo(11);
prints("foo res: ", res);
let s = "hello" + " world";
prints(s);
s = s + " 1134";
let x = 12;
let y = 3 * ((x + 2) * (9 - 10));
y = 2.5 + 1;

let test = x * y / 9;
{
	let g = print(s, x);
	prints("print res: ", g);
	let miha = 15.0 / x;
	x = miha * y;
	{
		miha = 0;
	}
}

let miha = 1;
